// 실제 값을 채운 뒤 이 파일 이름을 mesh_secrets.h 로 바꿔서 같은 폴더에 두세요.
// mesh_secrets.h 는 .gitignore에 등록되어 있어 git에는 올라가지 않습니다.
// D 보드(브릿지 역할)만 실제로 사용한다. A/B/C는 컴파일만 되고 안 쓰인다.
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASS "your_wifi_password"
#define MQTT_HOST "192.168.0.35"
#define MQTT_PORT 1883
